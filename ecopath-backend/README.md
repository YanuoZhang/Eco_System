# Ecopath Backend

## Development

- Copy `.env.example` to `.env` (optional)
- Install deps: `npm install`
- Start dev: `npm run dev`

## Endpoints
- `GET /healthz` → `{ status: "ok" }`
- `GET /api/environment` → `{ env: "development" }`
